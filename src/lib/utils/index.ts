// import styles from '@/components/modal/dropdown/dropdown.module.css'

// export function initDropdown() {
//     console.log("Dropdown initialization");

//     const openDropdownButtons = document.querySelectorAll(".open_dropdown");
//     const dropdown = document.getElementById('dropdown_overlay');

//     openDropdownButtons.forEach((button, index) => {
//         button.addEventListener("click", (e: Event) => {
//             if (dropdown?.classList.contains("is_active")) {
//                 dropdown?.classList.remove("is_active");
//             }
//             dropdown?.classList.add(styles["is_active"]);
//             dropdown?.classList.add(styles["is_active"]);
//         });
//     });
// }
