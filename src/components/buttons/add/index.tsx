// 'use client'

// import { useFormState } from "react-dom";

// interface ButtonProperties {
//     label: string;
//     color?: string;
// }

// export const AddButton = ({ label }: ButtonProperties) => {
//     const { pending } = useFormState();

//     return (
//         <button type="submit" disabled={pending}>
//             {pending ? 'Loading...' : label}
//         </button>
//     );
// }