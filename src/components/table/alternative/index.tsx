import patients from "@/json/patients.json"
import { Patient } from "@/types/patient.type";
import { useState } from "react";

export const TanStackTable = () => {

    const [data, setData] = useState<Patient[]>();
    return (
        <>
        </>
    );
}